SELECT 
    `extension` AS `homePhone%` ,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`destination`, ',', 2), ',', -1) AS `telephoneNumber%`
FROM 
    incoming
WHERE 
    `destination` LIKE 'from-did-direct,%';
